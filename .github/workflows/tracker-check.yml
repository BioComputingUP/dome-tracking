# DOME tracker: check every resource in tracker/resources.yml for uptime every 20 minutes,
# record the code activity of each resource's GitHub repository, and open/close an `outage`
# issue when a resource goes down/comes back.
# Code comes from main (tracker/ only); results are committed to the `tracker-data` branch,
# squashed to one commit per UTC day, so main's history is never touched by the tracker.
# The branch is also the GitHub Pages source, so pushing to it republishes the dashboard.
name: tracker-check

on:
  # Primary trigger: an external cron (cron-job.org) calls workflow_dispatch at :05/:25/:45,
  # because GitHub's own scheduler drops most ticks (tracker/README.md "Check interval").
  # These crons are a best-effort backup if that trigger stops. Worst case is 3 + 4 = 7 runs an
  # hour, under the GitHub Pages soft limit of 10 builds an hour.
  schedule:
    - cron: "7 * * * *"
    - cron: "23 * * * *"
    - cron: "39 * * * *"
    - cron: "51 * * * *"
  workflow_dispatch:
    inputs:
      simulate_down:
        description: "Test the outage alerts: resource id to treat as down (data is not changed)"
        required: false
        default: ""

permissions:
  contents: write
  issues: write

# Runs never overlap. If a run is still going when another is triggered, GitHub keeps only the
# newest pending one; that is fine, because every run is a full snapshot.
concurrency:
  group: tracker-check
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tracker code (tracker/ only)
        uses: actions/checkout@v4
        with:
          path: repo
          sparse-checkout: tracker

      - name: Check out data branch
        uses: actions/checkout@v4
        with:
          ref: tracker-data
          path: _data
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: repo/tracker/requirements.txt

      - name: Install
        run: pip install -r repo/tracker/requirements.txt

      - name: Run uptime checks
        run: python repo/tracker/scripts/check.py --config repo/tracker/resources.yml --data _data

      - name: Record repository activity
        env:
          # The built-in token can only read this repository and public ones. ACTIVITY_TOKEN, if
          # set, is a read-only fine-grained token for the private monitored repos (tracker/README.md).
          GITHUB_TOKEN: ${{ secrets.ACTIVITY_TOKEN || secrets.GITHUB_TOKEN }}
        run: python repo/tracker/scripts/activity.py --config repo/tracker/resources.yml --data _data

      - name: Summarise
        run: python repo/tracker/scripts/summarize.py --config repo/tracker/resources.yml --data _data

      - name: Publish the dashboard
        run: |
          cp repo/tracker/dashboard/index.html _data/index.html
          touch _data/.nojekyll   # serve paths starting with _ and skip the Jekyll build

      - name: Show the status in this run's summary
        run: |
          python - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json
          s = json.load(open("_data/summary.json"))
          t, m = s["totals"], s["monitoring"]
          print(f"## {'🔴' if m['stale'] else '🟢'} {s['resource_count']} resources checked\n")
          print(f"🟢 up **{t['up']}** · 🟡 challenged **{t['challenged']}** · "
                f"🔴 down **{t['down']}** · 🔗 URL review **{t['url_review']}**\n")
          print(f"Run {m['runs_total']} · {m['runs_last_24h']} runs in the last 24 h · {s['generated']}\n")
          print("[Dashboard](https://biocomputingup.github.io/dome-tracking/) · "
                "[STATUS.md](https://github.com/BioComputingUP/dome-tracking/blob/tracker-data/STATUS.md)\n")
          down = [r for r in s["resources"] if r["state"] == "down"]
          if down:
              print(f"<details><summary>{len(down)} down</summary>\n")
              print("| Resource | Reason | Since |\n|---|---|---|")
              for r in sorted(down, key=lambda r: r["name"].lower()):
                  print(f"| {r['name']} | `{r['reason']}` | {r['since']} |")
              print("\n</details>\n")
          repos = s["activity"]["repos"]
          if repos:
              print("| Repository | Last push | Activity |\n|---|---|---|")
              for rid, a in repos.items():
                  print(f"| {a['repo']} | {a.get('pushed_at') or '—'} | {a.get('freshness')}"
                        f"{' (API error)' if a.get('error') else ''} |")
          PY

      - name: Commit results (one commit per UTC day)
        working-directory: _data
        run: |
          bot_email="41898282+github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git config user.email "$bot_email"
          git add -A
          if git diff --cached --quiet; then echo "No changes"; exit 0; fi
          lease=$(git rev-parse HEAD)
          subject="tracker data $(date -u +%F)"
          if [ "$(git log -1 --format=%s)" = "$subject" ] && [ "$(git log -1 --format=%ae)" = "$bot_email" ]; then
            git commit --amend --no-edit --quiet
          else
            git commit -m "$subject" --quiet
          fi
          git push --force-with-lease="tracker-data:$lease" origin HEAD:tracker-data

      # After the commit, so the results are saved even if the GitHub API fails here. It must use
      # the built-in token: issues opened with your own token would not notify you.
      - name: Open or close outage issues
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SIMULATE_DOWN: ${{ inputs.simulate_down }}
        run: |
          python repo/tracker/scripts/notify.py --config repo/tracker/resources.yml --data _data \
            ${SIMULATE_DOWN:+--simulate-down "$SIMULATE_DOWN"}
